h1 Look at this sick nasty homepage {{message_to_send}}
form
  input(type='text' ng-model='message_to_send')
  input(type='submit' value='Startup' ng-click='startup()')
form
  input(type='submit' value='Logout' ng-click='logout()')
div(ng-repeat='point in data track by $index') {{point}}